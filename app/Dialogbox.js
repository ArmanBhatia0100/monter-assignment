
const Dialogbox = ()=>{
     return (
     <div className="dialogbox container max-w-3xl mx-auto bg-white p-4 rounded-lg shadow">
        {/* Title of the dialog box with with filter and close button */}
        <header></header>
        {/* Main data imported in the table */}
        <footer></footer>
        {/* custom paginator with a row selector */}
     </div>
     )
   }
   
   export default Dialogbox;